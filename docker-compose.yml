services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: echomind-api
    env_file:
      - .env.backend
    ports:
      - "127.0.0.1:18000:8000"
    restart: unless-stopped

